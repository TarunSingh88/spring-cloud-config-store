server:
  port: 9191
  jpa:
    hibernate:
      ddl-auto: update
security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9192/oauth/check_token
    client:
      client-id: mobile
      client-secret: password
